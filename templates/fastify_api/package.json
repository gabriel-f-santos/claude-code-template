{
  "name": "fastify-vibecoding-api",
  "version": "1.0.0",
  "description": "🚀 Fastify API Template for Vibecoding - Perfect for rapid CRUD development with Claude Code",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/app.js",
    "start": "node src/app.js",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "keywords": [
    "fastify",
    "api",
    "vibecoding",
    "claude-code",
    "crud",
    "prisma",
    "jwt"
  ],
  "author": "Vibecoding Team",
  "license": "MIT",
  "dependencies": {
    "fastify": "^4.24.3",
    "@fastify/swagger": "^8.12.0",
    "@fastify/swagger-ui": "^2.1.0",
    "@fastify/jwt": "^7.2.4",
    "@fastify/cors": "^8.4.0",
    "@fastify/rate-limit": "^8.0.3",
    "@fastify/env": "^4.2.0",
    "@fastify/helmet": "^11.1.1",
    "@prisma/client": "^5.7.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "prisma": "^5.7.0",
    "vitest": "^1.0.4",
    "@vitest/coverage-v8": "^1.0.4",
    "eslint": "^8.55.0",
    "@fastify/pre-commit": "^2.1.0",
    "pino-pretty": "^10.2.3"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}