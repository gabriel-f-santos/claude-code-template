# 🚀 FastAPI SQLAlchemy Vibecoding Configuration

# App Settings
APP_NAME="FastAPI SQLAlchemy Vibecoding"
VERSION="1.0.0"
DEBUG=true

# Database (choose one)
# SQLite (default - great for demos)
DATABASE_URL="sqlite:///./vibecoding.db"

# PostgreSQL (production ready)
# DATABASE_URL="postgresql://user:password@localhost:5432/vibecoding_db"

# MySQL (alternative)
# DATABASE_URL="mysql+pymysql://user:password@localhost:3306/vibecoding_db"

# Security (CHANGE IN PRODUCTION!)
SECRET_KEY="your-super-secret-key-change-in-production-please"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30